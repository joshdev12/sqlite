from tkinter import *

import backend as bk



# window = Tk()

# window.geometry("450x450")

# label1 = Label(text="Students", font=("Aria", 28, "bold"))
# label1.grid(row=0, column=0)

# textl = Text()
# textl.grid(row=1, column=0)

# window.mainloop()